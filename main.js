const operator = require('./features');

const sum = operator.add(1,2);
console.log(`The sum of 1 and 2 is ${sum}`);

const diff = operator.subtract(10,4);
console.log(`The difference of 10 and 4 is ${diff}`);